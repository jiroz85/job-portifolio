# Database Configuration (Auto-created by Node.js)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=job_portal

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRE=30d

# 🎯 Note: Database and tables are created automatically!
# Just run: npm start
